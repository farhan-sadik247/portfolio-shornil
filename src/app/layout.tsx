import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Anika Jahan Shornil | Microbiology Student",
  description: "Portfolio of Anika Jahan Shornil - Microbiology student at BRAC University. Passionate about microbiology research, laboratory sciences, and scientific innovation.",
  keywords: ["Anika Jahan Shornil", "Microbiology", "BRAC University", "Research", "Laboratory", "Science"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
